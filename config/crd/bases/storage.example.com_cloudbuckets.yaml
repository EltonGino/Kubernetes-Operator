---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: cloudbuckets.storage.example.com
spec:
  group: storage.example.com
  names:
    kind: CloudBucket
    listKind: CloudBucketList
    plural: cloudbuckets
    singular: cloudbucket
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CloudBucket is the Schema for the cloudbuckets API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CloudBucketSpec defines the desired state of CloudBucket.
            properties:
              foo:
                description: Foo is an example field of CloudBucket. Edit cloudbucket_types.go
                  to remove/update
                type: string
            type: object
          status:
            description: CloudBucketStatus defines the observed state of CloudBucket.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
